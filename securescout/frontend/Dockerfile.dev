FROM node:20-alpine

WORKDIR /app

# Install dependencies
COPY package*.json ./
RUN npm ci

# Expose Vite port
EXPOSE 3000

# Start Vite dev server (with polling for Docker volume changes)
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
